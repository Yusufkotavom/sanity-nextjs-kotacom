import {
  Decoration,
  EditorView,
  HighlightStyle,
  StreamLanguage,
  bracketMatching,
  crosshairCursor,
  defaultHighlightStyle,
  drawSelection,
  dropCursor,
  foldGutter,
  foldKeymap,
  highlightActiveLine,
  highlightActiveLineGutter,
  highlightSpecialChars,
  indentOnInput,
  indentUnit,
  keymap,
  lineNumbers,
  placeholder,
  rectangularSelection,
  syntaxHighlighting
} from "./chunk-BGKHV5RK.js";
import {
  defaultKeymap,
  highlightSelectionMatches,
  history,
  historyKeymap,
  indentWithTab,
  lintKeymap,
  oneDark,
  searchKeymap
} from "./chunk-VVAQPHFI.js";
import {
  autocompletion,
  closeBrackets,
  closeBracketsKeymap,
  completionKeymap
} from "./chunk-YU4RCDUE.js";
import {
  Annotation,
  EditorState,
  StateEffect,
  StateField,
  tags
} from "./chunk-DXGK2BI4.js";
import {
  CodeInputConfigContext
} from "./chunk-MA3GNSQX.js";
import {
  _extends,
  _objectWithoutPropertiesLoose
} from "./chunk-RAV62YKW.js";
import "./chunk-PL5TQWPV.js";
import {
  require_compiler_runtime
} from "./chunk-SH3HS3ET.js";
import "./chunk-3CJ6L2TA.js";
import "./chunk-Z2PYR4DL.js";
import {
  rem,
  rgba,
  useRootTheme,
  useTheme
} from "./chunk-3ZVCNXKR.js";
import "./chunk-BRE7IFDH.js";
import "./chunk-OKZEHT5I.js";
import "./chunk-6EEA7UTI.js";
import {
  require_jsx_runtime
} from "./chunk-IM6XUC6U.js";
import {
  require_react
} from "./chunk-SBCHFULI.js";
import {
  __toESM
} from "./chunk-PR4QN5HX.js";

// ../node_modules/.pnpm/@sanity+code-input@7.0.12_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@codemi_eb920092cac86edfdd144297e8c65e19/node_modules/@sanity/code-input/dist/_chunks-es/CodeMirrorProxy.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_compiler_runtime = __toESM(require_compiler_runtime(), 1);

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/index.js
var import_react2 = __toESM(require_react());

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/useCodeMirror.js
var import_react = __toESM(require_react(), 1);

// ../node_modules/.pnpm/@uiw+codemirror-extensions-basic-setup@4.25.8_@codemirror+autocomplete@6.20.1_@codemirr_c981c1b54fcc692e583f6e403a222554/node_modules/@uiw/codemirror-extensions-basic-setup/esm/index.js
var basicSetup = function basicSetup2(options) {
  if (options === void 0) {
    options = {};
  }
  var {
    crosshairCursor: initCrosshairCursor = false
  } = options;
  var keymaps = [];
  if (options.closeBracketsKeymap !== false) {
    keymaps = keymaps.concat(closeBracketsKeymap);
  }
  if (options.defaultKeymap !== false) {
    keymaps = keymaps.concat(defaultKeymap);
  }
  if (options.searchKeymap !== false) {
    keymaps = keymaps.concat(searchKeymap);
  }
  if (options.historyKeymap !== false) {
    keymaps = keymaps.concat(historyKeymap);
  }
  if (options.foldKeymap !== false) {
    keymaps = keymaps.concat(foldKeymap);
  }
  if (options.completionKeymap !== false) {
    keymaps = keymaps.concat(completionKeymap);
  }
  if (options.lintKeymap !== false) {
    keymaps = keymaps.concat(lintKeymap);
  }
  var extensions = [];
  if (options.lineNumbers !== false) extensions.push(lineNumbers());
  if (options.highlightActiveLineGutter !== false) extensions.push(highlightActiveLineGutter());
  if (options.highlightSpecialChars !== false) extensions.push(highlightSpecialChars());
  if (options.history !== false) extensions.push(history());
  if (options.foldGutter !== false) extensions.push(foldGutter());
  if (options.drawSelection !== false) extensions.push(drawSelection());
  if (options.dropCursor !== false) extensions.push(dropCursor());
  if (options.allowMultipleSelections !== false) extensions.push(EditorState.allowMultipleSelections.of(true));
  if (options.indentOnInput !== false) extensions.push(indentOnInput());
  if (options.syntaxHighlighting !== false) extensions.push(syntaxHighlighting(defaultHighlightStyle, {
    fallback: true
  }));
  if (options.bracketMatching !== false) extensions.push(bracketMatching());
  if (options.closeBrackets !== false) extensions.push(closeBrackets());
  if (options.autocompletion !== false) extensions.push(autocompletion());
  if (options.rectangularSelection !== false) extensions.push(rectangularSelection());
  if (initCrosshairCursor !== false) extensions.push(crosshairCursor());
  if (options.highlightActiveLine !== false) extensions.push(highlightActiveLine());
  if (options.highlightSelectionMatches !== false) extensions.push(highlightSelectionMatches());
  if (options.tabSize && typeof options.tabSize === "number") extensions.push(indentUnit.of(" ".repeat(options.tabSize)));
  return extensions.concat([keymap.of(keymaps.flat())]).filter(Boolean);
};

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/theme/light.js
var defaultLightThemeOption = EditorView.theme({
  "&": {
    backgroundColor: "#fff"
  }
}, {
  dark: false
});

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/getDefaultExtensions.js
var getDefaultExtensions = function getDefaultExtensions2(optios) {
  if (optios === void 0) {
    optios = {};
  }
  var {
    indentWithTab: defaultIndentWithTab = true,
    editable = true,
    readOnly = false,
    theme = "light",
    placeholder: placeholderStr = "",
    basicSetup: defaultBasicSetup = true
  } = optios;
  var getExtensions = [];
  if (defaultIndentWithTab) {
    getExtensions.unshift(keymap.of([indentWithTab]));
  }
  if (defaultBasicSetup) {
    if (typeof defaultBasicSetup === "boolean") {
      getExtensions.unshift(basicSetup());
    } else {
      getExtensions.unshift(basicSetup(defaultBasicSetup));
    }
  }
  if (placeholderStr) {
    getExtensions.unshift(placeholder(placeholderStr));
  }
  switch (theme) {
    case "light":
      getExtensions.push(defaultLightThemeOption);
      break;
    case "dark":
      getExtensions.push(oneDark);
      break;
    case "none":
      break;
    default:
      getExtensions.push(theme);
      break;
  }
  if (editable === false) {
    getExtensions.push(EditorView.editable.of(false));
  }
  if (readOnly) {
    getExtensions.push(EditorState.readOnly.of(true));
  }
  return [...getExtensions];
};

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/utils.js
var getStatistics = (view) => {
  return {
    line: view.state.doc.lineAt(view.state.selection.main.from),
    lineCount: view.state.doc.lines,
    lineBreak: view.state.lineBreak,
    length: view.state.doc.length,
    readOnly: view.state.readOnly,
    tabSize: view.state.tabSize,
    selection: view.state.selection,
    selectionAsSingle: view.state.selection.asSingle().main,
    ranges: view.state.selection.ranges,
    selectionCode: view.state.sliceDoc(view.state.selection.main.from, view.state.selection.main.to),
    selections: view.state.selection.ranges.map((r) => view.state.sliceDoc(r.from, r.to)),
    selectedText: view.state.selection.ranges.some((r) => !r.empty)
  };
};

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/timeoutLatch.js
var TimeoutLatch = class {
  constructor(callback, timeoutMS) {
    this.timeLeftMS = void 0;
    this.timeoutMS = void 0;
    this.isCancelled = false;
    this.isTimeExhausted = false;
    this.callbacks = [];
    this.timeLeftMS = timeoutMS;
    this.timeoutMS = timeoutMS;
    this.callbacks.push(callback);
  }
  tick() {
    if (!this.isCancelled && !this.isTimeExhausted) {
      this.timeLeftMS--;
      if (this.timeLeftMS <= 0) {
        this.isTimeExhausted = true;
        var callbacks = this.callbacks.slice();
        this.callbacks.length = 0;
        callbacks.forEach((callback) => {
          try {
            callback();
          } catch (error) {
            console.error("TimeoutLatch callback error:", error);
          }
        });
      }
    }
  }
  cancel() {
    this.isCancelled = true;
    this.callbacks.length = 0;
  }
  reset() {
    this.timeLeftMS = this.timeoutMS;
    this.isCancelled = false;
    this.isTimeExhausted = false;
  }
  get isDone() {
    return this.isCancelled || this.isTimeExhausted;
  }
};
var Scheduler = class {
  constructor() {
    this.interval = null;
    this.latches = /* @__PURE__ */ new Set();
  }
  add(latch) {
    this.latches.add(latch);
    this.start();
  }
  remove(latch) {
    this.latches.delete(latch);
    if (this.latches.size === 0) {
      this.stop();
    }
  }
  start() {
    if (this.interval === null) {
      this.interval = setInterval(() => {
        this.latches.forEach((latch) => {
          latch.tick();
          if (latch.isDone) {
            this.remove(latch);
          }
        });
      }, 1);
    }
  }
  stop() {
    if (this.interval !== null) {
      clearInterval(this.interval);
      this.interval = null;
    }
  }
};
var globalScheduler = null;
var getScheduler = () => {
  if (typeof window === "undefined") {
    return new Scheduler();
  }
  if (!globalScheduler) {
    globalScheduler = new Scheduler();
  }
  return globalScheduler;
};

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/useCodeMirror.js
var ExternalChange = Annotation.define();
var TYPING_TIMOUT = 200;
var emptyExtensions = [];
function useCodeMirror(props) {
  var {
    value,
    selection,
    onChange,
    onStatistics,
    onCreateEditor,
    onUpdate,
    extensions = emptyExtensions,
    autoFocus,
    theme = "light",
    height = null,
    minHeight = null,
    maxHeight = null,
    width = null,
    minWidth = null,
    maxWidth = null,
    placeholder: placeholderStr = "",
    editable = true,
    readOnly = false,
    indentWithTab: defaultIndentWithTab = true,
    basicSetup: defaultBasicSetup = true,
    root,
    initialState
  } = props;
  var [container, setContainer] = (0, import_react.useState)();
  var [view, setView] = (0, import_react.useState)();
  var [state, setState] = (0, import_react.useState)();
  var typingLatch = (0, import_react.useState)(() => ({
    current: null
  }))[0];
  var pendingUpdate = (0, import_react.useState)(() => ({
    current: null
  }))[0];
  var defaultThemeOption = EditorView.theme({
    "&": {
      height,
      minHeight,
      maxHeight,
      width,
      minWidth,
      maxWidth
    },
    "& .cm-scroller": {
      height: "100% !important"
    }
  });
  var updateListener = EditorView.updateListener.of((vu) => {
    if (vu.docChanged && typeof onChange === "function" && // Fix echoing of the remote changes:
    // If transaction is market as remote we don't have to call `onChange` handler again
    !vu.transactions.some((tr) => tr.annotation(ExternalChange))) {
      if (typingLatch.current) {
        typingLatch.current.reset();
      } else {
        typingLatch.current = new TimeoutLatch(() => {
          if (pendingUpdate.current) {
            var forceUpdate = pendingUpdate.current;
            pendingUpdate.current = null;
            forceUpdate();
          }
          typingLatch.current = null;
        }, TYPING_TIMOUT);
        getScheduler().add(typingLatch.current);
      }
      var doc = vu.state.doc;
      var _value = doc.toString();
      onChange(_value, vu);
    }
    onStatistics && onStatistics(getStatistics(vu));
  });
  var defaultExtensions = getDefaultExtensions({
    theme,
    editable,
    readOnly,
    placeholder: placeholderStr,
    indentWithTab: defaultIndentWithTab,
    basicSetup: defaultBasicSetup
  });
  var getExtensions = [updateListener, defaultThemeOption, ...defaultExtensions];
  if (onUpdate && typeof onUpdate === "function") {
    getExtensions.push(EditorView.updateListener.of(onUpdate));
  }
  getExtensions = getExtensions.concat(extensions);
  (0, import_react.useLayoutEffect)(() => {
    if (container && !state) {
      var config = {
        doc: value,
        selection,
        extensions: getExtensions
      };
      var stateCurrent = initialState ? EditorState.fromJSON(initialState.json, config, initialState.fields) : EditorState.create(config);
      setState(stateCurrent);
      if (!view) {
        var viewCurrent = new EditorView({
          state: stateCurrent,
          parent: container,
          root
        });
        setView(viewCurrent);
        onCreateEditor && onCreateEditor(viewCurrent, stateCurrent);
      }
    }
    return () => {
      if (view) {
        setState(void 0);
        setView(void 0);
      }
    };
  }, [container, state]);
  (0, import_react.useEffect)(() => {
    if (props.container) {
      setContainer(props.container);
    }
  }, [props.container]);
  (0, import_react.useEffect)(() => () => {
    if (view) {
      view.destroy();
      setView(void 0);
    }
    if (typingLatch.current) {
      typingLatch.current.cancel();
      typingLatch.current = null;
    }
  }, [view]);
  (0, import_react.useEffect)(() => {
    if (autoFocus && view) {
      view.focus();
    }
  }, [autoFocus, view]);
  (0, import_react.useEffect)(() => {
    if (view) {
      view.dispatch({
        effects: StateEffect.reconfigure.of(getExtensions)
      });
    }
  }, [theme, extensions, height, minHeight, maxHeight, width, minWidth, maxWidth, placeholderStr, editable, readOnly, defaultIndentWithTab, defaultBasicSetup, onChange, onUpdate]);
  (0, import_react.useEffect)(() => {
    if (value === void 0) {
      return;
    }
    var currentValue = view ? view.state.doc.toString() : "";
    if (view && value !== currentValue) {
      var isTyping = typingLatch.current && !typingLatch.current.isDone;
      var forceUpdate = () => {
        if (view && value !== view.state.doc.toString()) {
          view.dispatch({
            changes: {
              from: 0,
              to: view.state.doc.toString().length,
              insert: value || ""
            },
            annotations: [ExternalChange.of(true)]
          });
        }
      };
      if (!isTyping) {
        forceUpdate();
      } else {
        pendingUpdate.current = forceUpdate;
      }
    }
  }, [value, view]);
  return {
    state,
    setState,
    view,
    setView,
    container,
    setContainer
  };
}

// ../node_modules/.pnpm/@uiw+react-codemirror@4.25.8_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@cod_422e4b3433a1a4d0ac81e0b12b958e64/node_modules/@uiw/react-codemirror/esm/index.js
var import_jsx_runtime = __toESM(require_jsx_runtime());
var _excluded = ["className", "value", "selection", "extensions", "onChange", "onStatistics", "onCreateEditor", "onUpdate", "autoFocus", "theme", "height", "minHeight", "maxHeight", "width", "minWidth", "maxWidth", "basicSetup", "placeholder", "indentWithTab", "editable", "readOnly", "root", "initialState"];
var ReactCodeMirror = (0, import_react2.forwardRef)((props, ref) => {
  var {
    className,
    value = "",
    selection,
    extensions = [],
    onChange,
    onStatistics,
    onCreateEditor,
    onUpdate,
    autoFocus,
    theme = "light",
    height,
    minHeight,
    maxHeight,
    width,
    minWidth,
    maxWidth,
    basicSetup: basicSetup3,
    placeholder: placeholder2,
    indentWithTab: indentWithTab2,
    editable,
    readOnly,
    root,
    initialState
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded);
  var editor = (0, import_react2.useRef)(null);
  var {
    state,
    view,
    container,
    setContainer
  } = useCodeMirror({
    root,
    value,
    autoFocus,
    theme,
    height,
    minHeight,
    maxHeight,
    width,
    minWidth,
    maxWidth,
    basicSetup: basicSetup3,
    placeholder: placeholder2,
    indentWithTab: indentWithTab2,
    editable,
    readOnly,
    selection,
    onChange,
    onStatistics,
    onCreateEditor,
    onUpdate,
    extensions,
    initialState
  });
  (0, import_react2.useImperativeHandle)(ref, () => ({
    editor: editor.current,
    state,
    view
  }), [editor, container, state, view]);
  var setEditorRef = (0, import_react2.useCallback)((el) => {
    editor.current = el;
    setContainer(el);
  }, [setContainer]);
  if (typeof value !== "string") {
    throw new Error("value must be typeof string but got " + typeof value);
  }
  var defaultClassNames = typeof theme === "string" ? "cm-theme-" + theme : "cm-theme";
  return (0, import_jsx_runtime.jsx)("div", _extends({
    ref: setEditorRef,
    className: "" + defaultClassNames + (className ? " " + className : "")
  }, other));
});
ReactCodeMirror.displayName = "CodeMirror";
var esm_default = ReactCodeMirror;

// ../node_modules/.pnpm/@sanity+code-input@7.0.12_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@codemi_eb920092cac86edfdd144297e8c65e19/node_modules/@sanity/code-input/dist/_chunks-es/CodeMirrorProxy.js
var import_react3 = __toESM(require_react(), 1);

// ../node_modules/.pnpm/@uiw+codemirror-themes@4.25.8_@codemirror+language@6.12.2_@codemirror+state@6.6.0_@codemirror+view@6.40.0/node_modules/@uiw/codemirror-themes/esm/index.js
var createTheme = (_ref) => {
  var {
    theme,
    settings = {},
    styles = []
  } = _ref;
  var themeOptions = {
    ".cm-gutters": {}
  };
  var baseStyle = {};
  if (settings.background) {
    baseStyle.backgroundColor = settings.background;
  }
  if (settings.backgroundImage) {
    baseStyle.backgroundImage = settings.backgroundImage;
  }
  if (settings.foreground) {
    baseStyle.color = settings.foreground;
  }
  if (settings.fontSize) {
    baseStyle.fontSize = settings.fontSize;
  }
  if (settings.background || settings.foreground) {
    themeOptions["&"] = baseStyle;
  }
  if (settings.fontFamily) {
    themeOptions["&.cm-editor .cm-scroller"] = {
      fontFamily: settings.fontFamily
    };
  }
  if (settings.gutterBackground) {
    themeOptions[".cm-gutters"].backgroundColor = settings.gutterBackground;
  }
  if (settings.gutterForeground) {
    themeOptions[".cm-gutters"].color = settings.gutterForeground;
  }
  if (settings.gutterBorder) {
    themeOptions[".cm-gutters"].borderRightColor = settings.gutterBorder;
  }
  if (settings.caret) {
    themeOptions[".cm-content"] = {
      caretColor: settings.caret
    };
    themeOptions[".cm-cursor, .cm-dropCursor"] = {
      borderLeftColor: settings.caret
    };
  }
  var activeLineGutterStyle = {};
  if (settings.gutterActiveForeground) {
    activeLineGutterStyle.color = settings.gutterActiveForeground;
  }
  if (settings.lineHighlight) {
    themeOptions[".cm-activeLine"] = {
      backgroundColor: settings.lineHighlight
    };
    activeLineGutterStyle.backgroundColor = settings.lineHighlight;
  }
  themeOptions[".cm-activeLineGutter"] = activeLineGutterStyle;
  if (settings.selection) {
    themeOptions["&.cm-focused .cm-selectionBackground, & .cm-line::selection, & .cm-selectionLayer .cm-selectionBackground, .cm-content ::selection"] = {
      background: settings.selection + " !important"
    };
  }
  if (settings.selectionMatch) {
    themeOptions["& .cm-selectionMatch"] = {
      backgroundColor: settings.selectionMatch
    };
  }
  var themeExtension = EditorView.theme(themeOptions, {
    dark: theme === "dark"
  });
  var highlightStyle = HighlightStyle.define(styles);
  var extension = [themeExtension, syntaxHighlighting(highlightStyle)];
  return extension;
};

// ../node_modules/.pnpm/@sanity+code-input@7.0.12_@babel+runtime@7.29.2_@codemirror+autocomplete@6.20.1_@codemi_eb920092cac86edfdd144297e8c65e19/node_modules/@sanity/code-input/dist/_chunks-es/CodeMirrorProxy.js
var defaultCodeModes = [{
  name: "groq",
  loader: () => import("./dist-5X5KUFCO.js").then(({
    javascriptLanguage
  }) => javascriptLanguage)
}, {
  name: "javascript",
  loader: () => import("./dist-5X5KUFCO.js").then(({
    javascript
  }) => javascript({
    jsx: false
  }))
}, {
  name: "jsx",
  loader: () => import("./dist-5X5KUFCO.js").then(({
    javascript
  }) => javascript({
    jsx: true
  }))
}, {
  name: "typescript",
  loader: () => import("./dist-5X5KUFCO.js").then(({
    javascript
  }) => javascript({
    jsx: false,
    typescript: true
  }))
}, {
  name: "tsx",
  loader: () => import("./dist-5X5KUFCO.js").then(({
    javascript
  }) => javascript({
    jsx: true,
    typescript: true
  }))
}, {
  name: "php",
  loader: () => import("./dist-O6NWTNNH.js").then(({
    php
  }) => php())
}, {
  name: "sql",
  loader: () => import("./dist-BKY5WHOY.js").then(({
    sql
  }) => sql())
}, {
  name: "mysql",
  loader: () => import("./dist-BKY5WHOY.js").then(({
    sql,
    MySQL
  }) => sql({
    dialect: MySQL
  }))
}, {
  name: "json",
  loader: () => import("./dist-AZ3AE7TC.js").then(({
    json
  }) => json())
}, {
  name: "markdown",
  loader: () => import("./dist-ZOX3MKBB.js").then(({
    markdown
  }) => markdown())
}, {
  name: "java",
  loader: () => import("./dist-M6GVJVHC.js").then(({
    java
  }) => java())
}, {
  name: "html",
  loader: () => import("./dist-YQD5FZ2W.js").then(({
    html
  }) => html())
}, {
  name: "csharp",
  loader: () => import("./clike-3K5NWE6U.js").then(({
    csharp
  }) => StreamLanguage.define(csharp))
}, {
  name: "sh",
  loader: () => import("./shell-A262CG3E.js").then(({
    shell
  }) => StreamLanguage.define(shell))
}, {
  name: "css",
  loader: () => import("./css-5MEZH3VY.js").then(({
    css
  }) => StreamLanguage.define(css))
}, {
  name: "scss",
  loader: () => import("./css-5MEZH3VY.js").then(({
    css
  }) => StreamLanguage.define(css))
}, {
  name: "sass",
  loader: () => import("./sass-A5MO2UIX.js").then(({
    sass
  }) => StreamLanguage.define(sass))
}, {
  name: "ruby",
  loader: () => import("./ruby-N4IBDBB6.js").then(({
    ruby
  }) => StreamLanguage.define(ruby))
}, {
  name: "python",
  loader: () => import("./python-2YZW3GCE.js").then(({
    python
  }) => StreamLanguage.define(python))
}, {
  name: "xml",
  loader: () => import("./xml-2YRN7XQF.js").then(({
    xml
  }) => StreamLanguage.define(xml))
}, {
  name: "yaml",
  loader: () => import("./yaml-45M3VFHZ.js").then(({
    yaml
  }) => StreamLanguage.define(yaml))
}, {
  name: "golang",
  loader: () => import("./go-XLYFJCTM.js").then(({
    go
  }) => StreamLanguage.define(go))
}, {
  name: "text",
  loader: () => {
  }
}, {
  name: "batch",
  loader: () => {
  }
}];
function getBackwardsCompatibleTone(themeCtx) {
  return themeCtx.tone !== "neutral" && themeCtx.tone !== "suggest" ? themeCtx.tone : themeCtx.tone === "neutral" ? "default" : "primary";
}
var highlightLineClass = "cm-highlight-line";
var addLineHighlight = StateEffect.define();
var removeLineHighlight = StateEffect.define();
var lineHighlightField = StateField.define({
  create() {
    return Decoration.none;
  },
  update(lines, tr) {
    lines = lines.map(tr.changes);
    for (const e of tr.effects)
      e.is(addLineHighlight) && (lines = lines.update({
        add: [lineHighlightMark.range(e.value)]
      })), e.is(removeLineHighlight) && (lines = lines.update({
        filter: (from) => from !== e.value
      }));
    return lines;
  },
  toJSON(value, state) {
    const highlightLines = [], iter = value.iter();
    for (; iter.value; ) {
      const lineNumber = state.doc.lineAt(iter.from).number;
      highlightLines.includes(lineNumber) || highlightLines.push(lineNumber), iter.next();
    }
    return highlightLines;
  },
  fromJSON(value, state) {
    const lines = state.doc.lines, highlights = value.filter((line) => line <= lines).map((line) => lineHighlightMark.range(state.doc.line(line).from));
    highlights.sort((a, b) => a.from - b.from);
    try {
      return Decoration.none.update({
        add: highlights
      });
    } catch (e) {
      return console.error(e), Decoration.none;
    }
  },
  provide: (f) => EditorView.decorations.from(f)
});
var lineHighlightMark = Decoration.line({
  class: highlightLineClass
});
var highlightState = {
  highlight: lineHighlightField
};
function createCodeMirrorTheme(options) {
  const {
    themeCtx
  } = options, fallbackTone = getBackwardsCompatibleTone(themeCtx), dark = {
    color: themeCtx.theme.color.dark[fallbackTone]
  }, light = {
    color: themeCtx.theme.color.light[fallbackTone]
  };
  return EditorView.baseTheme({
    ".cm-lineNumbers": {
      cursor: "default"
    },
    ".cm-line.cm-line": {
      position: "relative"
    },
    // need set background with pseudoelement so it does not render over selection color
    [`.${highlightLineClass}::before`]: {
      position: "absolute",
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      zIndex: -3,
      content: "''",
      boxSizing: "border-box"
    },
    [`&dark .${highlightLineClass}::before`]: {
      background: rgba(dark.color.muted.caution.pressed.bg, 0.5)
    },
    [`&light .${highlightLineClass}::before`]: {
      background: rgba(light.color.muted.caution.pressed.bg, 0.75)
    }
  });
}
var highlightLine = (config) => {
  const highlightTheme = createCodeMirrorTheme({
    themeCtx: config.theme
  });
  return [lineHighlightField, config.readOnly ? [] : lineNumbers({
    domEventHandlers: {
      mousedown: (editorView, lineInfo) => {
        const line = editorView.state.doc.lineAt(lineInfo.from);
        let isHighlighted = false;
        return editorView.state.field(lineHighlightField).between(line.from, line.to, (_from, _to, value) => {
          if (value)
            return isHighlighted = true, false;
        }), isHighlighted ? editorView.dispatch({
          effects: removeLineHighlight.of(line.from)
        }) : editorView.dispatch({
          effects: addLineHighlight.of(line.from)
        }), config?.onHighlightChange && config.onHighlightChange(editorView.state.toJSON(highlightState).highlight), true;
      }
    }
  }), highlightTheme];
};
function setHighlightedLines(view, highlightLines) {
  const doc = view.state.doc, lines = doc.lines, allLineNumbers = Array.from({
    length: lines
  }, (_x, i) => i + 1);
  view.dispatch({
    effects: allLineNumbers.map((lineNumber) => {
      const line = doc.line(lineNumber);
      return highlightLines?.includes(lineNumber) ? addLineHighlight.of(line.from) : removeLineHighlight.of(line.from);
    })
  });
}
function useThemeExtension() {
  const $ = (0, import_compiler_runtime.c)(6), themeCtx = useRootTheme();
  let t0;
  $[0] !== themeCtx ? (t0 = getBackwardsCompatibleTone(themeCtx), $[0] = themeCtx, $[1] = t0) : t0 = $[1];
  const fallbackTone = t0, t1 = themeCtx.theme.color.dark[fallbackTone];
  let t2;
  if ($[2] !== fallbackTone || $[3] !== t1 || $[4] !== themeCtx.theme.color.light) {
    const dark = {
      color: t1
    }, light = {
      color: themeCtx.theme.color.light[fallbackTone]
    };
    t2 = EditorView.baseTheme({
      "&.cm-editor": {
        height: "100%"
      },
      "&.cm-editor.cm-focused": {
        outline: "none"
      },
      "&.cm-editor.cm-focused .cm-matchingBracket": {
        backgroundColor: "transparent"
      },
      "&.cm-editor.cm-focused .cm-nonmatchingBracket": {
        backgroundColor: "transparent"
      },
      "&dark.cm-editor.cm-focused .cm-matchingBracket": {
        outline: `1px solid ${dark.color.base.border}`
      },
      "&dark.cm-editor.cm-focused .cm-nonmatchingBracket": {
        outline: `1px solid ${dark.color.base.border}`
      },
      "&light.cm-editor.cm-focused .cm-matchingBracket": {
        outline: `1px solid ${light.color.base.border}`
      },
      "&light.cm-editor.cm-focused .cm-nonmatchingBracket": {
        outline: `1px solid ${light.color.base.border}`
      },
      "& .cm-lineNumbers .cm-gutterElement": {
        minWidth: "32px !important",
        padding: "0 8px !important"
      },
      "& .cm-gutter.cm-foldGutter": {
        width: "0px !important"
      },
      "&dark .cm-gutters": {
        color: `${rgba(dark.color.card.enabled.code.fg, 0.5)} !important`,
        borderRight: `1px solid ${rgba(dark.color.base.border, 0.5)}`
      },
      "&light .cm-gutters": {
        color: `${rgba(light.color.card.enabled.code.fg, 0.5)} !important`,
        borderRight: `1px solid ${rgba(light.color.base.border, 0.5)}`
      }
    }), $[2] = fallbackTone, $[3] = t1, $[4] = themeCtx.theme.color.light, $[5] = t2;
  } else
    t2 = $[5];
  return t2;
}
function useCodeMirrorTheme() {
  const $ = (0, import_compiler_runtime.c)(19), theme = useTheme(), {
    code: codeFont
  } = theme.sanity.fonts, {
    base,
    card,
    dark,
    syntax
  } = theme.sanity.color, t0 = dark ? "dark" : "light";
  let t1;
  return $[0] !== base.focusRing || $[1] !== card.disabled.bg || $[2] !== card.disabled.code.fg || $[3] !== card.enabled.bg || $[4] !== card.enabled.code.fg || $[5] !== card.enabled.fg || $[6] !== codeFont.family || $[7] !== syntax.attrName || $[8] !== syntax.boolean || $[9] !== syntax.className || $[10] !== syntax.comment || $[11] !== syntax.function || $[12] !== syntax.keyword || $[13] !== syntax.number || $[14] !== syntax.operator || $[15] !== syntax.property || $[16] !== syntax.string || $[17] !== t0 ? (t1 = createTheme({
    theme: t0,
    settings: {
      background: card.enabled.bg,
      foreground: card.enabled.code.fg,
      lineHighlight: card.enabled.bg,
      fontFamily: codeFont.family,
      caret: base.focusRing,
      selection: rgba(base.focusRing, 0.2),
      selectionMatch: rgba(base.focusRing, 0.4),
      gutterBackground: card.disabled.bg,
      gutterForeground: card.disabled.code.fg,
      gutterActiveForeground: card.enabled.fg
    },
    styles: [{
      tag: [tags.heading, tags.heading2, tags.heading3, tags.heading4, tags.heading5, tags.heading6],
      color: card.enabled.fg
    }, {
      tag: tags.angleBracket,
      color: card.enabled.code.fg
    }, {
      tag: tags.atom,
      color: syntax.keyword
    }, {
      tag: tags.attributeName,
      color: syntax.attrName
    }, {
      tag: tags.bool,
      color: syntax.boolean
    }, {
      tag: tags.bracket,
      color: card.enabled.code.fg
    }, {
      tag: tags.className,
      color: syntax.className
    }, {
      tag: tags.comment,
      color: syntax.comment
    }, {
      tag: tags.definition(tags.typeName),
      color: syntax.function
    }, {
      tag: [tags.definition(tags.variableName), tags.function(tags.variableName), tags.className, tags.attributeName],
      color: syntax.function
    }, {
      tag: [tags.function(tags.propertyName), tags.propertyName],
      color: syntax.function
    }, {
      tag: tags.keyword,
      color: syntax.keyword
    }, {
      tag: tags.null,
      color: syntax.number
    }, {
      tag: tags.number,
      color: syntax.number
    }, {
      tag: tags.meta,
      color: card.enabled.code.fg
    }, {
      tag: tags.operator,
      color: syntax.operator
    }, {
      tag: tags.propertyName,
      color: syntax.property
    }, {
      tag: [tags.string, tags.special(tags.brace)],
      color: syntax.string
    }, {
      tag: tags.tagName,
      color: syntax.className
    }, {
      tag: tags.typeName,
      color: syntax.keyword
    }]
  }), $[0] = base.focusRing, $[1] = card.disabled.bg, $[2] = card.disabled.code.fg, $[3] = card.enabled.bg, $[4] = card.enabled.code.fg, $[5] = card.enabled.fg, $[6] = codeFont.family, $[7] = syntax.attrName, $[8] = syntax.boolean, $[9] = syntax.className, $[10] = syntax.comment, $[11] = syntax.function, $[12] = syntax.keyword, $[13] = syntax.number, $[14] = syntax.operator, $[15] = syntax.property, $[16] = syntax.string, $[17] = t0, $[18] = t1) : t1 = $[18], t1;
}
function useFontSizeExtension(props) {
  const $ = (0, import_compiler_runtime.c)(3), {
    fontSize: fontSizeProp
  } = props, theme = useTheme(), {
    code: codeFont
  } = theme.sanity.fonts, {
    fontSize,
    lineHeight
  } = codeFont.sizes[fontSizeProp] || codeFont.sizes[2];
  let t0;
  return $[0] !== fontSize || $[1] !== lineHeight ? (t0 = EditorView.baseTheme({
    "&": {
      fontSize: rem(fontSize)
    },
    "& .cm-scroller": {
      lineHeight: `${lineHeight / fontSize} !important`
    }
  }), $[0] = fontSize, $[1] = lineHeight, $[2] = t0) : t0 = $[2], t0;
}
var CodeMirrorProxy = (0, import_react3.forwardRef)(function(props, ref) {
  const $ = (0, import_compiler_runtime.c)(41);
  let basicSetupProp, codeMirrorProps, highlightLines, languageMode, onHighlightChange, readOnly, value;
  $[0] !== props ? ({
    basicSetup: basicSetupProp,
    highlightLines,
    languageMode,
    onHighlightChange,
    readOnly,
    value,
    ...codeMirrorProps
  } = props, $[0] = props, $[1] = basicSetupProp, $[2] = codeMirrorProps, $[3] = highlightLines, $[4] = languageMode, $[5] = onHighlightChange, $[6] = readOnly, $[7] = value) : (basicSetupProp = $[1], codeMirrorProps = $[2], highlightLines = $[3], languageMode = $[4], onHighlightChange = $[5], readOnly = $[6], value = $[7]);
  const themeCtx = useRootTheme(), codeMirrorTheme = useCodeMirrorTheme(), [editorView, setEditorView] = (0, import_react3.useState)(void 0), themeExtension = useThemeExtension();
  let t0;
  $[8] === /* @__PURE__ */ Symbol.for("react.memo_cache_sentinel") ? (t0 = {
    fontSize: 1
  }, $[8] = t0) : t0 = $[8];
  const fontSizeExtension = useFontSizeExtension(t0), languageExtension = useLanguageExtension(languageMode);
  let t1;
  $[9] !== onHighlightChange || $[10] !== readOnly || $[11] !== themeCtx ? (t1 = highlightLine({
    onHighlightChange,
    readOnly,
    theme: themeCtx
  }), $[9] = onHighlightChange, $[10] = readOnly, $[11] = themeCtx, $[12] = t1) : t1 = $[12];
  const highlightLineExtension = t1;
  let t2;
  bb0: {
    let t32;
    $[13] !== fontSizeExtension || $[14] !== highlightLineExtension || $[15] !== themeExtension ? (t32 = [themeExtension, fontSizeExtension, highlightLineExtension, EditorView.lineWrapping], $[13] = fontSizeExtension, $[14] = highlightLineExtension, $[15] = themeExtension, $[16] = t32) : t32 = $[16];
    const baseExtensions = t32;
    if (languageExtension) {
      let t42;
      $[17] !== baseExtensions || $[18] !== languageExtension ? (t42 = [...baseExtensions, languageExtension], $[17] = baseExtensions, $[18] = languageExtension, $[19] = t42) : t42 = $[19], t2 = t42;
      break bb0;
    }
    t2 = baseExtensions;
  }
  const extensions = t2;
  let t3;
  $[20] !== editorView || $[21] !== highlightLines ? (t3 = () => {
    editorView && setHighlightedLines(editorView, highlightLines ?? []);
  }, $[20] = editorView, $[21] = highlightLines, $[22] = t3) : t3 = $[22];
  let t4;
  $[23] !== editorView || $[24] !== highlightLines || $[25] !== value ? (t4 = [editorView, highlightLines, value], $[23] = editorView, $[24] = highlightLines, $[25] = value, $[26] = t4) : t4 = $[26], (0, import_react3.useEffect)(t3, t4);
  let t5;
  $[27] !== highlightLines || $[28] !== value ? (t5 = () => ({
    json: {
      doc: value ?? "",
      selection: {
        main: 0,
        ranges: [{
          anchor: 0,
          head: 0
        }]
      },
      highlight: highlightLines ?? []
    },
    fields: highlightState
  }), $[27] = highlightLines, $[28] = value, $[29] = t5) : t5 = $[29];
  const [initialState] = (0, import_react3.useState)(t5);
  let t6;
  $[30] === /* @__PURE__ */ Symbol.for("react.memo_cache_sentinel") ? (t6 = (view) => {
    setEditorView(view);
  }, $[30] = t6) : t6 = $[30];
  const handleCreateEditor = t6;
  let t7;
  $[31] !== basicSetupProp ? (t7 = basicSetupProp ?? {
    highlightActiveLine: false
  }, $[31] = basicSetupProp, $[32] = t7) : t7 = $[32];
  const basicSetup3 = t7;
  let t8;
  return $[33] !== basicSetup3 || $[34] !== codeMirrorProps || $[35] !== codeMirrorTheme || $[36] !== extensions || $[37] !== initialState || $[38] !== ref || $[39] !== value ? (t8 = (0, import_jsx_runtime2.jsx)(esm_default, { ...codeMirrorProps, value, ref, extensions, theme: codeMirrorTheme, onCreateEditor: handleCreateEditor, initialState, basicSetup: basicSetup3 }), $[33] = basicSetup3, $[34] = codeMirrorProps, $[35] = codeMirrorTheme, $[36] = extensions, $[37] = initialState, $[38] = ref, $[39] = value, $[40] = t8) : t8 = $[40], t8;
});
function useLanguageExtension(mode) {
  const $ = (0, import_compiler_runtime.c)(6), codeConfig = (0, import_react3.useContext)(CodeInputConfigContext), [languageExtension, setLanguageExtension] = (0, import_react3.useState)();
  let t0;
  $[0] !== codeConfig?.codeModes || $[1] !== mode ? (t0 = () => {
    const codeMode = [...codeConfig?.codeModes ?? [], ...defaultCodeModes].find((m) => m.name === mode);
    codeMode?.loader || console.warn(`Found no codeMode for language mode ${mode}, syntax highlighting will be disabled.`);
    let active = true;
    return Promise.resolve(codeMode?.loader()).then((extension) => {
      active && setLanguageExtension(extension);
    }).catch((e) => {
      console.error(`Failed to load language mode ${mode}`, e), active && setLanguageExtension(void 0);
    }), () => {
      active = false;
    };
  }, $[0] = codeConfig?.codeModes, $[1] = mode, $[2] = t0) : t0 = $[2];
  let t1;
  return $[3] !== codeConfig || $[4] !== mode ? (t1 = [mode, codeConfig], $[3] = codeConfig, $[4] = mode, $[5] = t1) : t1 = $[5], (0, import_react3.useEffect)(t0, t1), languageExtension;
}
export {
  CodeMirrorProxy as default
};
//# sourceMappingURL=CodeMirrorProxy-C75CGOI3.js.map
