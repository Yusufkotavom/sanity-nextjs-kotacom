# Legacy Rewrite Archive v0

Folder ini menyimpan komponen rewrite generasi lama yang sudah tidak menjadi jalur aktif.

Status:

- diarsipkan untuk referensi historis
- bukan source of truth visual
- jangan dipakai untuk route aktif baru

Source of truth yang aktif sekarang:

- `frontend/components/ui/rewrite/*`
- `frontend/components/blocks/*` yang sudah diselaraskan ke tone rewrite
