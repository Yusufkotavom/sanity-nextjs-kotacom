# Script Catalog

1. `frontend/scripts/ops-check-block-contract.mjs`
   - cek sinkronisasi block type Studio vs GROQ vs renderer.
2. `frontend/scripts/ops-validate-sanity-payload.mjs`
   - validasi payload Sanity + autofix opsional.
3. `scripts/build-repo-daily-ops-skill-zip.mjs`
   - validasi struktur skill + generate `skill.zip`.
