# Workflow

1. Generate payload (`ops-generate-content-payload`).
2. Validate payload (`ops-validate-sanity-payload`).
3. Dry-run upsert (`ops-upsert-sanity-content`).
4. Write mode bila sudah lolos validasi dan disetujui.
